*.asi
*.exe
*.dll
*.obj
*.lib
*.pdb

# Anchored to the build locations. Unanchored bin/ and build/ would also match
# layout/ph_ft/work/bin, which is the installed file tree and must be published.
/asi/build/
/manager/bin/
/manager/obj/
/manager/build/

# Copied out of asi/vendor by sdk/tools-source/build-checker.sh, next to the
# checker it licenses. The original is published, this copy is a build output.
/sdk/LICENSES/
